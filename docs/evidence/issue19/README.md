# Issue 19 acceptance evidence — 2026-09-16

Implementation snapshot: `aa93ec291ce08298c88a001fb63654859f427c06`, based on
`05a9a0b5d414e4ed29f84abc7a2f799e3f33c9d3`. Both manifests observed this clean
checkout. Driver source remains `db3014f9499694c6f186af7e023de07bd5bc3564`
(r11); this change does not incorporate later driver implementation fixes.

## Reproducibility and identities

Command: `./tools/reproduce-package.sh /tmp/issue19-repro-aa93ec2`.
The two separate writable build trees share the read-only host toolchain and a fixed
`/build` path. Both execute with networking disabled, `SOURCE_DATE_EPOCH=1789590567`,
UTC and C.UTF-8. This is controlled same-toolchain reproducibility, not an independent
clean-chroot or cross-toolchain claim.

Both builds pass all 35 Meson cases. Both complete package archives are byte-identical:
`3a5ecffebd4a07219b4e10d49bc7d78c93212de341c43b623eb388ff5ced1969`.
Both stripped drivers have SHA-256
`a9d6225e0fd348ca22fd738cb2367d7835d7cda1f789ba7b5aae278b32b6729b`, matching
the [r11 record](../../codec-validation-r11-2026-09-15.json) exactly, with the same
source commit. That links the existing record's tests and limitations; it is not a new
hardware run or broader support claim. A different driver hash must not inherit it.

The earlier exploratory builds used the base commit's timestamp; their full archive
hash differs because `.PKGINFO`/`.BUILDINFO` and archive timestamps follow
`SOURCE_DATE_EPOCH`. The stripped driver was unchanged. No unexplained difference
occurred between either controlled pair.

- [First manifest](one.json), [second manifest](two.json): source, recipe, patch,
  package and driver identities; exported ABI; dependency bounds; root ownership;
  alias; complete `.BUILDINFO`; inspector identity; kernel/header observations.
- [Comparison](comparison.json), [effective makepkg configuration](makepkg.conf),
  [first build log](one.log), [second build log](two.log).
- The package warning about `$srcdir` references is from shipped README examples,
  not a compiled driver path difference. Both builds emit the same warning.
- Archives and source mirror were retained locally at the command's output path;
  they are not distributed in this repository.

Build host: aarch64 Apple `j413` / `t8112` (M2), Linux `7.1.13-3-1-ARCH`;
`linux-asahi` and `linux-asahi-headers` both `7.1.13.asahi3-1`. Tool/package
versions are recorded in `.BUILDINFO`; GCC 16.1.1, binutils 2.46, Meson 1.12.0,
libva 2.24.1 and libdrm 2.4.134. The selected on-disk module hash is an observation
only. No module was loaded at observation; loaded binary identity is not established.

## Offline regression checks

Passed on the implementation snapshot:

```sh
bash -n install.sh uninstall.sh bin/apple-avd-rebuild tests/rebuild.sh \
  tests/installer.sh libva/PKGBUILD libva/libva-v4l2_request-avd.install \
  tools/reproduce-package.sh
bash tests/rebuild.sh
bash tests/installer.sh
python3 tests/h264-interlace-scan-test.py
python3 tests/package-provenance-test.py
```

The Python provenance suite uses `jsonschema` in a disposable virtual environment;
12 tests pass, as do all 12 H.264 scanner tests. Both generated manifests validate
against [the schema](../../package-provenance.schema.json). The r11 historical
fixture normalizes with absent provenance left null.

Before the health fix, a text file containing the marker and an entrypoint name
incorrectly passed. Compiled ELF fixtures now cover supported/older/newer ABI,
unknown package major, multiple entrypoints, undefined/data/hidden symbols,
wrong marker, missing driver, and simulated next-load module replacement while
loaded identity stays unknown. Archive regressions cover non-root ownership,
wrong alias, missing bounds, recipe mismatch, path traversal, duplicate members,
wrong/non-ELF driver, undefined entrypoint, mismatched hardware identities, and
preserving existing output files on failure. Installer regressions still pass.

## Limits and handoff

No installation, module build/reload, reboot, hardware decode, browser/display or
boot-stability validation was performed. No shipped kernel patch changed. A source
upgrade to accepted later driver fixes is a separate candidate and must establish
its own package/stripped-driver evidence. Merge and maintainer release acceptance
remain pending; this evidence does not close the issue by itself.

This evidence summary was generated by AI; machine output is retained alongside it.
